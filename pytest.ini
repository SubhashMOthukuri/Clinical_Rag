[pytest]
asyncio_mode = auto

markers =
    e2e: live end-to-end tests (require running server + real API keys)
    unit: fast unit tests with no external dependencies

# Default: run only unit tests
# To run e2e: pytest tests/e2e/ -v --timeout=60
testpaths = tests/units
